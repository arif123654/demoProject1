This is my demo file for learning Git basics. Hello world........

At initial:

git add .
git init
git config user.name "username"
git config user.email "example@example.com"
git commit -m "Commits"
git remote add origin github profile link / git remote set-url origin github profilelink
git push -u origin master


For Update:
git add .
git commit -m "updated commit"
git push origin master


For Pull:



For pod:
sudo gem install cocoapods
Pod init
Open pod file
Pod install

For Pod Deinit:
$ sudo gem install cocoapods-deintegrate cocoapods-clean
$ pod deintegrate
$ pod clean
$ rm Podfile




https://riptutorial.com/ios/example/10868/embed-a-view-controller-in-a-navigation-controller-programmatically
